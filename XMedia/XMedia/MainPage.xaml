<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XMedia"
             xmlns:converter="clr-namespace:XMedia.Converter" 
             x:Class="XMedia.MainPage">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converter:ByteImageSourceValueConverter x:Key="ByteImageConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    
    <ListView
        ItemsSource="{Binding MediaFiles}"
        HasUnevenRows="True">

        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Orientation="Vertical">
                        <Label Text="{Binding FileName}"/>
                        <Image Source="{Binding Data}" HeightRequest="50" WidthRequest="50"/>
                        
                    </StackLayout>
                </ViewCell>
                
                
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</ContentPage>
